<%= form_with(model: event_attending, local: true) do |form| %>
  <% if event_attending.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_attending.errors.count, "error") %> prohibited this event_attending from being saved:</h2>

      <ul>
        <% event_attending.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :attended_event_id %>
    <%= form.number_field :attended_event_id %>
  </div>

  <div class="field">
    <%= form.label :attending_user_id %>
    <%= form.number_field :attending_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
